{# bearish trend × medium IV — controlled downtrend #}
{{ ticker }} is trading at ${{ "%.2f"|format(underlying_at_open) }}, below the 20-day simple moving average of ${{ "%.2f"|format(sma20_at_open) }}. The price structure reflects a measured descent with orderly retracements that fail to reclaim prior swing highs — a controlled downtrend rather than a disorderly selloff. Implied volatility measures at the {{ iv_percentile_at_open }}th percentile, placing option premiums in the middle of their observed distribution over the past twelve months. This moderate pricing level allows for a balanced approach to strike selection where neither chasing premium nor accepting thin credit is necessary.

{{ vol_regime_modifier }} In this configuration, a 14-day Iron Condor generates a moderate credit that must be assessed alongside the underlying directional lean. The structure is designed as a directionally neutral position, but the bearish context means the put-side risk warrants a closer look during sizing. The premium is sufficient to construct a breakeven range that extends beyond the typical two-week expected move without requiring the wings to be placed at extreme distances.

The 14-day ATR of ${{ "%.2f"|format(atr14_at_open) }} determines the statistical spacing for the short strikes. Setting the short call approximately 1.5 ATRs above the current level and the short put a comparable distance below produces a capture zone that historically encompasses the majority of two-week price trajectories. The wings are sized to cap the theoretical maximum loss while keeping the capital at risk proportional to the opportunity presented.
