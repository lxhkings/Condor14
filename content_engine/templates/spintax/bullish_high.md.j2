{# bullish trend × high IV — uptrend with rich premiums #}
{{ ticker }} closed at ${{ "%.2f"|format(underlying_at_open) }}, sitting above its 20-day simple moving average of ${{ "%.2f"|format(sma20_at_open) }}. The daily-chart structure remains constructive, with each pullback so far holding above prior swing lows. Realized volatility registers at the {{ vol_percentile_at_open }}th percentile of its trailing 12-month range, which places premiums in unusually rich territory relative to recent realized moves. The combination of trend and volatility creates a setup where the seller receives elevated compensation for a structure that does not require continued directional momentum to succeed.

{{ vol_regime_modifier }} For a 14-day Iron Condor, the math favors the seller in this configuration: time decay accelerates while implied volatility has historical pressure to revert lower. The structure thrives when the underlying drifts inside the short strikes, regardless of which direction it leans, and the rich premium translates directly into a wider breakeven envelope.

The 14-day ATR of ${{ "%.2f"|format(atr14_at_open) }} provides the statistical anchor for strike selection. Anchoring the short call at roughly 1.5 ATRs above spot keeps it outside the typical two-week move while still capturing meaningful credit, and the symmetric put-side anchor balances the structure. Risk is bounded by wing width minus the credit received; the position is not directional in intent, only in selection bias when trend filters apply.

{% include 'spintax/_track_record_line.md.j2' %}
